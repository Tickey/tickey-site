import React from 'react';
import classes from './NotFound.scss';

export default function NotFound() {
    return (
        <section className="wrapper main-side-space centered">
            <h2>Не е намерена страница</h2>
        </section>
    );
}
